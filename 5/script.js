// Adiciona um evento de clique ao botão
document.getElementById('meubotao').addEventListener('click', function() {

    // Checa o elemento a ser alterado
    var elementToChange = document.getElementById('meubotao')
    
    // Ele ira mudar a cor de fundo do elemento para amarelo
    elementToChange.style.backgroundColor = 'yellow'
})
