const par = document.getElementById('oloco');
 const numero = ["carlos ama demais a sua namorada evellyn, muito mesmo"];
const maior = numero.map(numero => numero.toUpperCase());
 par.innerHTML = (maior);